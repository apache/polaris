apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "polaris.fullname" . }}-test-connection"
  labels:
    helm.sh/chart: {{ include "polaris.chart" . }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    {{- if .Chart.AppVersion }}
    app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
    {{- end }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "polaris.fullname" . | printf "%s-metrics" }}:{{ index .Values.service.ports "polaris-metrics" }}/healthcheck']
  restartPolicy: Never