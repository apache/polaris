{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "properties": {
        "advancedConfig": {
            "description": "Advanced configuration. You can pass here any valid Polaris or Quarkus configuration property. Any property that is defined here takes precedence over all the other configuration values generated by this chart. Properties can be passed \"flattened\" or as nested YAML objects (see examples below). Note: values should be strings; avoid using numbers, booleans, or other types.",
            "type": "object"
        },
        "affinity": {
            "description": "Affinity and anti-affinity for polaris pods. See https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#affinity-and-anti-affinity.",
            "type": "object"
        },
        "authentication": {
            "description": "Polaris authentication configuration.",
            "type": "object",
            "properties": {
                "authenticator": {
                    "description": "The `Authenticator` implementation to use. Only one built-in type is supported: default.",
                    "type": "object",
                    "properties": {
                        "type": {
                            "description": "The type of authenticator to use.",
                            "type": "string"
                        }
                    }
                },
                "realmOverrides": {
                    "description": "Authentication configuration overrides per realm.",
                    "type": "object"
                },
                "tokenBroker": {
                    "description": "The `TokenBroker` implementation to use. Two built-in types are supported: rsa-key-pair and symmetric-key. Only relevant when using internal (or mixed) authentication. When using external authentication, the token broker is not used.",
                    "type": "object",
                    "properties": {
                        "maxTokenGeneration": {
                            "description": "Maximum token generation duration (e.g., PT1H for 1 hour).",
                            "type": "string"
                        },
                        "secret": {
                            "description": "The secret name to pull the public and private keys, or the symmetric key secret from.",
                            "type": "object",
                            "properties": {
                                "name": {
                                    "description": "The name of the secret to pull the keys from. If not provided, a key pair will be generated. This is not recommended for production.",
                                    "type": "string"
                                },
                                "privateKey": {
                                    "description": "DEPRECATED: Use `authentication.tokenBroker.secret.rsaKeyPair.privateKey` instead. Key name inside the secret for the private key",
                                    "type": "string"
                                },
                                "publicKey": {
                                    "description": "DEPRECATED: Use `authentication.tokenBroker.secret.rsaKeyPair.publicKey` instead. Key name inside the secret for the public key",
                                    "type": "string"
                                },
                                "rsaKeyPair": {
                                    "description": "Optional: configuration specific to RSA key pair secret.",
                                    "type": "object",
                                    "properties": {
                                        "privateKey": {
                                            "description": "Key name inside the secret for the private key",
                                            "type": "string"
                                        },
                                        "publicKey": {
                                            "description": "Key name inside the secret for the public key",
                                            "type": "string"
                                        }
                                    }
                                },
                                "secretKey": {
                                    "description": "DEPRECATED: Use `authentication.tokenBroker.secret.symmetricKey.secretKey` instead. Key name inside the secret for the symmetric key",
                                    "type": "string"
                                },
                                "symmetricKey": {
                                    "description": "Optional: configuration specific to symmetric key secret.",
                                    "type": "object",
                                    "properties": {
                                        "secretKey": {
                                            "description": "Key name inside the secret for the symmetric key",
                                            "type": "string"
                                        }
                                    }
                                }
                            }
                        },
                        "type": {
                            "description": "The type of token broker to use.",
                            "type": "string"
                        }
                    }
                },
                "tokenService": {
                    "description": "The token service (`IcebergRestOAuth2ApiService`) implementation to use. Two built-in types are supported: default and disabled. Only relevant when using internal (or mixed) authentication. When using external authentication, the token service is always disabled.",
                    "type": "object",
                    "properties": {
                        "type": {
                            "description": "The type of token service to use.",
                            "type": "string"
                        }
                    }
                },
                "type": {
                    "description": "The type of authentication to use. Three built-in types are supported: internal, external, and mixed.",
                    "type": "string",
                    "enum": [
                        "internal",
                        "external",
                        "mixed"
                    ]
                }
            }
        },
        "autoscaling": {
            "type": "object",
            "properties": {
                "enabled": {
                    "description": "Specifies whether automatic horizontal scaling should be enabled. Do not enable this when using in-memory version store type.",
                    "type": "boolean"
                },
                "maxReplicas": {
                    "description": "The maximum number of replicas to maintain.",
                    "type": "integer"
                },
                "minReplicas": {
                    "description": "The minimum number of replicas to maintain.",
                    "type": "integer"
                },
                "targetCPUUtilizationPercentage": {
                    "description": "Optional; set to zero or empty to disable.",
                    "type": "integer"
                },
                "targetMemoryUtilizationPercentage": {
                    "description": "Optional; set to zero or empty to disable.",
                    "type": "integer"
                }
            }
        },
        "configMapLabels": {
            "description": "Additional Labels to apply to polaris configmap.",
            "type": "object",
            "additionalProperties": {
                "type": "string"
            }
        },
        "containerSecurityContext": {
            "description": "Security context for the polaris container. See https://kubernetes.io/docs/tasks/configure-pod-container/security-context/.",
            "type": "object"
        },
        "cors": {
            "description": "Polaris CORS configuration.",
            "type": "object",
            "properties": {
                "accessControlAllowCredentials": {
                    "description": "The `Access-Control-Allow-Credentials` response header. The value of this header will default to `true` if `allowedOrigins` property is set and there is a match with the precise `Origin` header.",
                    "type": [
                        "boolean",
                        "null"
                    ]
                },
                "accessControlMaxAge": {
                    "description": "The `Access-Control-Max-Age` response header value indicating how long the results of a pre-flight request can be cached. Must be a valid duration.",
                    "type": "string"
                },
                "allowedHeaders": {
                    "description": "HTTP headers allowed for CORS, ex: X-Custom, Content-Disposition. If this is not set or empty, all requested headers are considered allowed.",
                    "type": "array",
                    "items": {
                        "type": "string"
                    }
                },
                "allowedMethods": {
                    "description": "HTTP methods allowed for CORS, ex: GET, PUT, POST. If this is not set or empty, all requested methods are considered allowed.",
                    "type": "array",
                    "items": {
                        "type": "string"
                    }
                },
                "allowedOrigins": {
                    "description": "Origins allowed for CORS, e.g. http://polaris.apache.org, http://localhost:8181. In case an entry of the list is surrounded by forward slashes, it is interpreted as a regular expression.",
                    "type": "array",
                    "items": {
                        "type": "string"
                    }
                },
                "exposedHeaders": {
                    "description": "HTTP headers exposed to the client, ex: X-Custom, Content-Disposition. The default is an empty list.",
                    "type": "array",
                    "items": {
                        "type": "string"
                    }
                }
            }
        },
        "extraEnv": {
            "description": "Advanced configuration via Environment Variables. Extra environment variables to add to the Polaris server container. You can pass here any valid EnvVar object: https://kubernetes.io/docs/reference/generated/kubernetes-api/v1.27/#envvar-v1-core This can be useful to get configuration values from Kubernetes secrets or config maps.",
            "type": "array",
            "items": {
                "type": "object"
            }
        },
        "extraInitContainers": {
            "description": "Add additional init containers to the polaris pod(s) See https://kubernetes.io/docs/concepts/workloads/pods/init-containers/.",
            "type": "array",
            "items": {
                "type": "object"
            }
        },
        "extraServices": {
            "description": "Additional service definitions. All service definitions always select all Polaris pods. Use this if you need to expose specific ports with different configurations, e.g. expose polaris-http with an alternate LoadBalancer service instead of ClusterIP.",
            "type": "array",
            "items": {
                "type": "object",
                "required": [
                    "nameSuffix"
                ],
                "properties": {
                    "annotations": {
                        "description": "Annotations to add to the extra service.",
                        "type": "object",
                        "additionalProperties": {
                            "type": "string"
                        }
                    },
                    "clusterIP": {
                        "description": "The cluster IP for the extra service.",
                        "type": "string"
                    },
                    "externalTrafficPolicy": {
                        "description": "Controls how traffic from external sources is routed.",
                        "type": "string",
                        "enum": [
                            "Cluster",
                            "Local"
                        ]
                    },
                    "internalTrafficPolicy": {
                        "description": "Controls how traffic from internal sources is routed.",
                        "type": "string",
                        "enum": [
                            "Cluster",
                            "Local"
                        ]
                    },
                    "nameSuffix": {
                        "description": "The suffix to append to the service name. Required. It must be unique. If it does not start with a hyphen, a hyphen will be inserted between the base service name and the suffix.",
                        "type": "string"
                    },
                    "ports": {
                        "description": "The ports the extra service will listen on.",
                        "type": "array",
                        "minItems": 1,
                        "items": {
                            "type": "object",
                            "required": [
                                "name",
                                "port"
                            ],
                            "properties": {
                                "name": {
                                    "description": "The name of the port. Required.",
                                    "type": "string"
                                },
                                "nodePort": {
                                    "description": "The port on each node on which this extra service is exposed when type is NodePort or LoadBalancer. Usually assigned by the system. If not specified or zero, a port will be allocated if this Service requires one. If this field is specified when creating a Service which does not need it, creation will fail.",
                                    "type": "integer"
                                },
                                "port": {
                                    "description": "The port the extra service listens on.",
                                    "type": "integer"
                                },
                                "protocol": {
                                    "description": "The IP protocol for this port. Supports \"TCP\", \"UDP\", and \"SCTP\". Default is TCP.",
                                    "type": "string",
                                    "enum": [
                                        "TCP",
                                        "UDP",
                                        "SCTP"
                                    ]
                                },
                                "targetPort": {
                                    "description": "Number of the port to access on the pods targeted by the service. If this is not specified or zero, the value of the 'port' field is used.",
                                    "type": "integer"
                                }
                            }
                        }
                    },
                    "sessionAffinity": {
                        "description": "The session affinity for the extra service. Valid values are: None, ClientIP. The default value is None.",
                        "type": "string",
                        "enum": [
                            "None",
                            "ClientIP"
                        ]
                    },
                    "trafficDistribution": {
                        "description": "The traffic distribution field.",
                        "oneOf": [
                            {
                                "type": "string",
                                "enum": [
                                    "PreferClose"
                                ]
                            },
                            {
                                "type": "null"
                            }
                        ]
                    },
                    "type": {
                        "description": "The type of service to create. Valid values are: ExternalName, ClusterIP, NodePort, and LoadBalancer.",
                        "type": "string",
                        "enum": [
                            "ExternalName",
                            "ClusterIP",
                            "NodePort",
                            "LoadBalancer"
                        ]
                    }
                }
            }
        },
        "extraVolumeMounts": {
            "description": "Extra volume mounts to add to the polaris container. See https://kubernetes.io/docs/concepts/storage/volumes/.",
            "type": "array",
            "items": {
                "type": "object"
            }
        },
        "extraVolumes": {
            "description": "Extra volumes to add to the polaris pod. See https://kubernetes.io/docs/concepts/storage/volumes/.",
            "type": "array",
            "items": {
                "type": "object"
            }
        },
        "features": {
            "description": "Polaris features configuration.",
            "type": "object",
            "properties": {
                "realmOverrides": {
                    "description": "Features to enable or disable per realm. This field is a map of maps. The realm name is the key, and the value is a map of feature names to values. If a feature is not present in the map, the default value from the 'defaults' field is used.",
                    "type": "object"
                }
            }
        },
        "fileIo": {
            "description": "Polaris FileIO configuration.",
            "type": "object",
            "properties": {
                "type": {
                    "description": "The type of file IO to use. Two built-in types are supported: default and wasb. The wasb one translates WASB paths to ABFS ones.",
                    "type": "string"
                }
            }
        },
        "gateway": {
            "type": "object",
            "properties": {
                "addresses": {
                    "description": "Optional addresses to request for the Gateway.",
                    "type": "array",
                    "items": {
                        "type": "object"
                    }
                },
                "annotations": {
                    "description": "Annotations to add to the Gateway.",
                    "type": "object",
                    "additionalProperties": {
                        "type": "string"
                    }
                },
                "className": {
                    "description": "The name of the GatewayClass to use.",
                    "type": "string"
                },
                "enabled": {
                    "description": "Specifies whether a Gateway should be created.",
                    "type": "boolean"
                },
                "listeners": {
                    "description": "Gateway listeners configuration.",
                    "type": "array",
                    "minItems": 1,
                    "items": {
                        "type": "object",
                        "required": [
                            "name"
                        ],
                        "properties": {
                            "allowedRoutes": {
                                "description": "AllowedRoutes defines the types of routes that MAY be attached to a Listener and the trusted namespaces where those Route resources MAY be present.",
                                "type": "object"
                            },
                            "hostname": {
                                "description": "Hostname specifies the virtual hostname to match for protocol types that define this concept. When unspecified, all hostnames are matched.",
                                "type": "string"
                            },
                            "name": {
                                "description": "The name of the listener. Required.",
                                "type": "string"
                            },
                            "port": {
                                "description": "The port number to use for the listener.",
                                "type": "integer"
                            },
                            "protocol": {
                                "description": "Protocol specifies the network protocol this listener expects to receive.",
                                "type": "string"
                            }
                        }
                    }
                }
            }
        },
        "httproute": {
            "type": "object",
            "required": [
                "gatewayName",
                "gatewayNamespace"
            ],
            "properties": {
                "annotations": {
                    "description": "Annotations to add to the HTTPRoute.",
                    "type": "object",
                    "additionalProperties": {
                        "type": "string"
                    }
                },
                "enabled": {
                    "description": "Specifies whether an HTTPRoute should be created.",
                    "type": "boolean"
                },
                "gatewayName": {
                    "description": "Name of the Gateway resource to attach to. Required.",
                    "type": "string"
                },
                "gatewayNamespace": {
                    "description": "Namespace where the Gateway is deployed. Required.",
                    "type": "string"
                },
                "hosts": {
                    "description": "A list of hostnames that the HTTPRoute should match.",
                    "type": "array",
                    "items": {
                        "type": "string"
                    }
                },
                "sectionName": {
                    "description": "Section name within the gateway to use (optional).",
                    "type": "string"
                }
            }
        },
        "image": {
            "type": "object",
            "properties": {
                "configDir": {
                    "description": "The path to the directory where the application.properties file, and other configuration files, if any, should be mounted.",
                    "type": "string"
                },
                "pullPolicy": {
                    "description": "The image pull policy.",
                    "type": "string",
                    "enum": [
                        "Always",
                        "IfNotPresent",
                        "Never"
                    ]
                },
                "repository": {
                    "description": "The image repository to pull from.",
                    "type": "string"
                },
                "tag": {
                    "description": "The image tag.",
                    "type": "string"
                }
            }
        },
        "imagePullSecrets": {
            "description": "References to secrets in the same namespace to use for pulling any of the images used by this chart. Each entry is a string referring to an existing secret in the namespace. The secret must contain a .dockerconfigjson key with a base64-encoded Docker configuration file. See https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/ for more information.",
            "type": "array",
            "items": {
                "type": "string"
            }
        },
        "ingress": {
            "type": "object",
            "properties": {
                "annotations": {
                    "description": "Annotations to add to the ingress.",
                    "type": "object",
                    "additionalProperties": {
                        "type": "string"
                    }
                },
                "className": {
                    "description": "Specifies the ingressClassName; leave empty if you don't want to customize it.",
                    "type": "string"
                },
                "enabled": {
                    "description": "Specifies whether an ingress should be created.",
                    "type": "boolean"
                },
                "hosts": {
                    "description": "A list of host paths used to configure the ingress.",
                    "type": "array",
                    "items": {
                        "type": "object",
                        "required": [
                            "host",
                            "paths"
                        ],
                        "properties": {
                            "host": {
                                "description": "The host name. Required.",
                                "type": "string"
                            },
                            "paths": {
                                "description": "A list of paths used to configure the ingress.",
                                "type": "array",
                                "minItems": 1,
                                "items": {
                                    "type": "object",
                                    "properties": {
                                        "path": {
                                            "type": "string"
                                        },
                                        "pathType": {
                                            "description": "The type of path. Valid values are: Exact, Prefix, and ImplementationSpecific.",
                                            "type": "string",
                                            "enum": [
                                                "Exact",
                                                "Prefix",
                                                "ImplementationSpecific"
                                            ]
                                        }
                                    }
                                }
                            }
                        }
                    }
                },
                "tls": {
                    "description": "A list of TLS certificates; each entry has a list of hosts in the certificate, along with the secret name used to terminate TLS traffic on port 443.",
                    "type": "array",
                    "items": {
                        "type": "object",
                        "required": [
                            "secretName"
                        ],
                        "properties": {
                            "hosts": {
                                "description": "A list of hosts in the certificate.",
                                "type": "array",
                                "minItems": 1,
                                "items": {
                                    "type": "string"
                                }
                            },
                            "secretName": {
                                "description": "The name of the TLS secret. Required.",
                                "type": "string"
                            }
                        }
                    }
                }
            }
        },
        "livenessProbe": {
            "description": "Configures the liveness probe for polaris pods.",
            "type": "object",
            "properties": {
                "failureThreshold": {
                    "description": "Minimum consecutive failures for the probe to be considered failed after having succeeded. Minimum value is 1.",
                    "type": "integer",
                    "minimum": 1
                },
                "initialDelaySeconds": {
                    "description": "Number of seconds after the container has started before liveness probes are initiated. Minimum value is 0.",
                    "type": "integer",
                    "minimum": 0
                },
                "periodSeconds": {
                    "description": "How often (in seconds) to perform the probe. Minimum value is 1.",
                    "type": "integer",
                    "minimum": 1
                },
                "successThreshold": {
                    "description": "Minimum consecutive successes for the probe to be considered successful after having failed. Minimum value is 1.",
                    "type": "integer",
                    "minimum": 1
                },
                "terminationGracePeriodSeconds": {
                    "description": "Optional duration in seconds the pod needs to terminate gracefully upon probe failure. Minimum value is 1.",
                    "type": "integer",
                    "minimum": 1
                },
                "timeoutSeconds": {
                    "description": "Number of seconds after which the probe times out. Minimum value is 1.",
                    "type": "integer",
                    "minimum": 1
                }
            }
        },
        "logging": {
            "description": "Logging configuration.",
            "type": "object",
            "properties": {
                "categories": {
                    "description": "Configuration for specific log categories. Keys are category names (e.g., org.apache.polaris), values are log levels.",
                    "type": "object",
                    "properties": {
                        "org.apache.iceberg.rest": {
                            "type": "string"
                        },
                        "org.apache.polaris": {
                            "type": "string"
                        }
                    },
                    "additionalProperties": {
                        "type": [
                            "string",
                            "object"
                        ]
                    }
                },
                "console": {
                    "description": "Configuration for the console appender.",
                    "type": "object",
                    "properties": {
                        "enabled": {
                            "description": "Whether to enable the console appender.",
                            "type": "boolean"
                        },
                        "format": {
                            "description": "The log format to use. Ignored if JSON format is enabled. See https://quarkus.io/guides/logging#logging-format for details.",
                            "type": "string"
                        },
                        "json": {
                            "description": "Whether to log in JSON format.",
                            "type": "boolean"
                        },
                        "threshold": {
                            "description": "The log level of the console appender.",
                            "type": "string",
                            "enum": [
                                "TRACE",
                                "DEBUG",
                                "INFO",
                                "WARN",
                                "ERROR",
                                "OFF",
                                "ALL"
                            ]
                        }
                    }
                },
                "file": {
                    "description": "Configuration for the file appender.",
                    "type": "object",
                    "properties": {
                        "enabled": {
                            "description": "Whether to enable the file appender.",
                            "type": "boolean"
                        },
                        "fileName": {
                            "description": "The log file name.",
                            "type": "string"
                        },
                        "format": {
                            "description": "The log format to use. Ignored if JSON format is enabled. See https://quarkus.io/guides/logging#logging-format for details.",
                            "type": "string"
                        },
                        "json": {
                            "description": "Whether to log in JSON format.",
                            "type": "boolean"
                        },
                        "logsDir": {
                            "description": "The local directory where log files are stored. The persistent volume claim will be mounted here.",
                            "type": "string"
                        },
                        "rotation": {
                            "description": "Log rotation configuration.",
                            "type": "object",
                            "properties": {
                                "fileSuffix": {
                                    "description": "An optional suffix to append to the rotated log files. If present, the rotated log files will be grouped in time buckets, and each bucket will contain at most maxBackupIndex files. The suffix must be in a date-time format that is understood by DateTimeFormatter. If the suffix ends with .gz or .zip, the rotated files will also be compressed using the corresponding algorithm.",
                                    "type": [
                                        "string",
                                        "null"
                                    ]
                                },
                                "maxBackupIndex": {
                                    "description": "The maximum number of backup files to keep.",
                                    "type": "integer",
                                    "minimum": 1
                                },
                                "maxFileSize": {
                                    "description": "The maximum size of the log file before it is rotated. Should be expressed as a Kubernetes quantity.",
                                    "type": "string"
                                }
                            }
                        },
                        "storage": {
                            "description": "The log storage configuration. A persistent volume claim will be created using these settings.",
                            "type": "object",
                            "properties": {
                                "className": {
                                    "description": "The storage class name of the persistent volume claim to create.",
                                    "type": "string"
                                },
                                "selectorLabels": {
                                    "description": "Labels to add to the persistent volume claim spec selector; a persistent volume with matching labels must exist. Leave empty if using dynamic provisioning.",
                                    "type": "object",
                                    "additionalProperties": {
                                        "type": "string"
                                    }
                                },
                                "size": {
                                    "description": "The size of the persistent volume claim to create.",
                                    "type": "string"
                                }
                            }
                        },
                        "threshold": {
                            "description": "The log level of the file appender.",
                            "type": "string",
                            "enum": [
                                "TRACE",
                                "DEBUG",
                                "INFO",
                                "WARN",
                                "ERROR",
                                "OFF",
                                "ALL"
                            ]
                        }
                    }
                },
                "level": {
                    "description": "The log level of the root category, which is used as the default log level for all categories.",
                    "type": "string",
                    "enum": [
                        "TRACE",
                        "DEBUG",
                        "INFO",
                        "WARN",
                        "ERROR",
                        "OFF",
                        "ALL"
                    ]
                },
                "mdc": {
                    "description": "Configuration for MDC (Mapped Diagnostic Context). Values specified here will be added to the log context of all incoming requests and can be used in log patterns.",
                    "type": "object",
                    "additionalProperties": {
                        "type": [
                            "string",
                            "object"
                        ]
                    }
                },
                "requestIdHeaderName": {
                    "description": "The header name to use for the request ID.",
                    "type": "string"
                }
            }
        },
        "managementService": {
            "description": "Management service settings. These settings are used to configure liveness and readiness probes, and to configure the dedicated headless service that will expose health checks and metrics, e.g. for metrics scraping and service monitoring.",
            "type": "object",
            "properties": {
                "annotations": {
                    "description": "Annotations to add to the service.",
                    "type": "object",
                    "additionalProperties": {
                        "type": "string"
                    }
                },
                "clusterIP": {
                    "description": "By default, the management service is headless, i.e. it does not have a cluster IP. This is generally the right option for exposing health checks and metrics, e.g. for metrics scraping and service monitoring.",
                    "type": "string"
                },
                "externalTrafficPolicy": {
                    "description": "Controls how traffic from external sources is routed.",
                    "type": "string",
                    "enum": [
                        "Cluster",
                        "Local"
                    ]
                },
                "internalTrafficPolicy": {
                    "description": "Controls how traffic from internal sources is routed.",
                    "type": "string",
                    "enum": [
                        "Cluster",
                        "Local"
                    ]
                },
                "ports": {
                    "description": "The ports the management service will listen on. At least one port is required; the first port implicitly becomes the HTTP port that the application will use for serving management requests. By default, it's 8182. Note: port names must be unique and no more than 15 characters long.",
                    "type": "array",
                    "minItems": 1,
                    "items": {
                        "type": "object",
                        "required": [
                            "name",
                            "port"
                        ],
                        "properties": {
                            "name": {
                                "description": "The name of the management port. Required.",
                                "type": "string"
                            },
                            "nodePort": {
                                "description": "The port on each node on which this service is exposed when type is NodePort or LoadBalancer. Usually assigned by the system. If not specified or zero, a port will be allocated if this Service requires one. If this field is specified when creating a Service which does not need it, creation will fail.",
                                "type": "integer"
                            },
                            "port": {
                                "description": "The port the management service listens on. By default, the management interface is exposed on HTTP port 8182.",
                                "type": "integer"
                            },
                            "protocol": {
                                "description": "The IP protocol for this port. Supports \"TCP\", \"UDP\", and \"SCTP\". Default is TCP.",
                                "type": "string",
                                "enum": [
                                    "TCP",
                                    "UDP",
                                    "SCTP"
                                ]
                            },
                            "targetPort": {
                                "description": "Number of the port to access on the pods targeted by the service. If this is not specified or zero, the value of the 'port' field is used.",
                                "type": "integer"
                            }
                        }
                    }
                },
                "sessionAffinity": {
                    "description": "The session affinity for the service.",
                    "type": "string",
                    "enum": [
                        "None",
                        "ClientIP"
                    ]
                },
                "trafficDistribution": {
                    "description": "The traffic distribution field.",
                    "oneOf": [
                        {
                            "type": "string",
                            "enum": [
                                "PreferClose"
                            ]
                        },
                        {
                            "type": "null"
                        }
                    ]
                },
                "type": {
                    "description": "The type of service to create. Valid values are: ExternalName, ClusterIP, NodePort, and LoadBalancer. The default value is ClusterIP.",
                    "type": "string",
                    "enum": [
                        "ExternalName",
                        "ClusterIP",
                        "NodePort",
                        "LoadBalancer"
                    ]
                }
            }
        },
        "metrics": {
            "type": "object",
            "properties": {
                "enabled": {
                    "description": "Specifies whether metrics for the polaris server should be enabled.",
                    "type": "boolean"
                },
                "tags": {
                    "description": "Additional tags (dimensional labels) to add to the metrics.",
                    "type": "object",
                    "additionalProperties": {
                        "type": "string"
                    }
                }
            }
        },
        "nodeSelector": {
            "description": "Node labels which must match for the polaris pod to be scheduled on that node. See https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/#nodeselector.",
            "type": "object",
            "additionalProperties": {
                "type": "string"
            }
        },
        "oidc": {
            "description": "Polaris OIDC configuration. Only relevant when at least one realm is configured for external (or mixed) authentication. The currently supported configuration is for a single, default OIDC tenant. For more complex scenarios, including OIDC multi-tenancy, you will need to provide the relevant configuration using the `advancedConfig` section.",
            "type": "object",
            "properties": {
                "authServeUrl": {
                    "description": "The authentication server URL. Must be provided if at least one realm is configured for external authentication.",
                    "type": "string"
                },
                "client": {
                    "description": "The client to use when authenticating with the authentication server.",
                    "type": "object",
                    "properties": {
                        "id": {
                            "description": "The client ID to use when contacting the authentication server's introspection endpoint in order to validate tokens.",
                            "type": "string"
                        },
                        "secret": {
                            "description": "The secret to pull the client secret from. If no client secret is required, leave the secret name unset.",
                            "type": "object",
                            "properties": {
                                "key": {
                                    "description": "The key name inside the secret to pull the client secret from.",
                                    "type": "string"
                                },
                                "name": {
                                    "description": "The name of the secret to pull the client secret from. If not provided, the client is assumed to not require a client secret when contacting the introspection endpoint.",
                                    "type": "string"
                                }
                            }
                        }
                    }
                },
                "principalMapper": {
                    "description": "Principal mapping configuration.",
                    "type": "object",
                    "properties": {
                        "idClaimPath": {
                            "description": "The path to the claim that contains the principal ID. Nested paths can be expressed using \"/\" as a separator, e.g. \"polaris/principal_id\" would look for the \"principal_id\" field inside the \"polaris\" object in the token claims. Optional. Either this option or `nameClaimPath` (or both) must be provided.",
                            "type": "string"
                        },
                        "nameClaimPath": {
                            "description": "The claim that contains the principal name. Nested paths can be expressed using \"/\" as a separator, e.g. \"polaris/principal_name\" would look for the \"principal_name\" field inside the \"polaris\" object in the token claims. Optional. Either this option or `idClaimPath` (or both) must be provided.",
                            "type": "string"
                        },
                        "type": {
                            "description": "The `PrincipalMapper` implementation to use. Only one built-in type is supported: default.",
                            "type": "string"
                        }
                    }
                },
                "principalRolesMapper": {
                    "description": "Principal roles mapping configuration.",
                    "type": "object",
                    "properties": {
                        "filter": {
                            "description": "A regular expression that matches the role names in the identity. Only roles that match this regex will be included in the Polaris-specific roles.",
                            "type": "string"
                        },
                        "mappings": {
                            "description": "A list of regex mappings that will be applied to each role name in the identity. This can be used to transform the role names in the identity into role names as expected by Polaris. The default Authenticator expects the security identity to expose role names in the format `POLARIS_ROLE:\u003crole name\u003e`.",
                            "type": "array",
                            "items": {
                                "type": "object"
                            }
                        },
                        "rolesClaimPath": {
                            "description": "The path to the claim that contains the principal roles. Nested paths can be expressed using \"/\" as a separator, e.g. \"polaris/principal_roles\" would look for the \"principal_roles\" field inside the \"polaris\" object in the token claims. If not set, Quarkus looks for roles in standard locations. See https://quarkus.io/guides/security-oidc-bearer-token-authentication#token-claims-and-security-identity-roles.",
                            "type": "string"
                        },
                        "type": {
                            "description": "The `PrincipalRolesMapper` implementation to use. Only one built-in type is supported: default.",
                            "type": "string"
                        }
                    }
                }
            }
        },
        "persistence": {
            "description": "Polaris persistence configuration.",
            "type": "object",
            "properties": {
                "nosql": {
                    "type": "object",
                    "properties": {
                        "backend": {
                            "description": "The NoSQL backend to use. Two built-in types are supported: MongoDb and InMemory. Only MongoDb is supported for production use.",
                            "type": "string"
                        },
                        "database": {
                            "description": "The MongoDB database name to use.",
                            "type": "string"
                        },
                        "secret": {
                            "type": "object",
                            "properties": {
                                "connectionString": {
                                    "description": "The secret key holding the MongoDB connection string.",
                                    "type": "string"
                                },
                                "name": {
                                    "description": "The secret name to pull the MongoDB connection string from.",
                                    "type": "string"
                                }
                            }
                        }
                    }
                },
                "relationalJdbc": {
                    "description": "The configuration for the relational-jdbc persistence manager.",
                    "type": "object",
                    "properties": {
                        "secret": {
                            "description": "The secret name to pull the database connection properties from.",
                            "type": "object",
                            "properties": {
                                "jdbcUrl": {
                                    "description": "The secret key holding the database JDBC connection URL",
                                    "type": "string"
                                },
                                "name": {
                                    "description": "The secret name to pull database connection properties from",
                                    "type": "string"
                                },
                                "password": {
                                    "description": "The secret key holding the database password for authentication",
                                    "type": "string"
                                },
                                "username": {
                                    "description": "The secret key holding the database username for authentication",
                                    "type": "string"
                                }
                            }
                        }
                    }
                },
                "type": {
                    "description": "The type of persistence to use. Two built-in types are supported: in-memory and relational-jdbc.",
                    "type": "string"
                }
            }
        },
        "podAnnotations": {
            "description": "Annotations to apply to polaris pods.",
            "type": "object",
            "additionalProperties": {
                "type": "string"
            }
        },
        "podDisruptionBudget": {
            "description": "Pod disruption budget settings.",
            "type": "object",
            "properties": {
                "annotations": {
                    "description": "Annotations to add to the pod disruption budget.",
                    "type": "object",
                    "additionalProperties": {
                        "type": "string"
                    }
                },
                "enabled": {
                    "description": "Specifies whether a pod disruption budget should be created.",
                    "type": "boolean"
                },
                "maxUnavailable": {
                    "description": "The maximum number of pods that can be unavailable during disruptions. Can be an absolute number (ex: 5) or a percentage of desired pods (ex: 50%). IMPORTANT: Cannot be used simultaneously with minAvailable.",
                    "type": [
                        "string",
                        "integer"
                    ]
                },
                "minAvailable": {
                    "description": "The minimum number of pods that should remain available during disruptions. Can be an absolute number (ex: 5) or a percentage of desired pods (ex: 50%). IMPORTANT: Cannot be used simultaneously with maxUnavailable.",
                    "type": [
                        "string",
                        "integer"
                    ]
                }
            }
        },
        "podLabels": {
            "description": "Additional Labels to apply to polaris pods.",
            "type": "object",
            "additionalProperties": {
                "type": "string"
            }
        },
        "podSecurityContext": {
            "description": "Security context for the polaris pod. See https://kubernetes.io/docs/tasks/configure-pod-container/security-context/.",
            "type": "object"
        },
        "priorityClassName": {
            "description": "Priority class name for polaris pods. See https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#pod-priority",
            "type": "string"
        },
        "rateLimiter": {
            "description": "Polaris rate limiter configuration.",
            "type": "object",
            "properties": {
                "tokenBucket": {
                    "description": "The configuration for the default rate limiter, which uses the token bucket algorithm with one bucket per realm.",
                    "type": "object",
                    "properties": {
                        "requestsPerSecond": {
                            "description": "The maximum number of requests (permits) per second allowed for each realm.",
                            "type": "integer",
                            "minimum": 1
                        },
                        "type": {
                            "description": "The type of the token bucket rate limiter. Only the default type is supported out of the box.",
                            "type": "string"
                        }
                    }
                },
                "type": {
                    "description": "The type of rate limiter filter to use. Two built-in types are supported: default and no-op.",
                    "type": "string"
                }
            }
        },
        "readinessProbe": {
            "description": "Configures the readiness probe for polaris pods.",
            "type": "object",
            "properties": {
                "failureThreshold": {
                    "description": "Minimum consecutive failures for the probe to be considered failed after having succeeded. Minimum value is 1.",
                    "type": "integer",
                    "minimum": 1
                },
                "initialDelaySeconds": {
                    "description": "Number of seconds after the container has started before readiness probes are initiated. Minimum value is 0.",
                    "type": "integer",
                    "minimum": 0
                },
                "periodSeconds": {
                    "description": "How often (in seconds) to perform the probe. Minimum value is 1.",
                    "type": "integer",
                    "minimum": 1
                },
                "successThreshold": {
                    "description": "Minimum consecutive successes for the probe to be considered successful after having failed. Minimum value is 1.",
                    "type": "integer",
                    "minimum": 1
                },
                "timeoutSeconds": {
                    "description": "Number of seconds after which the probe times out. Minimum value is 1.",
                    "type": "integer",
                    "minimum": 1
                }
            }
        },
        "realmContext": {
            "description": "Realm context resolver configuration.",
            "type": "object",
            "properties": {
                "realms": {
                    "description": "List of valid realms, for use with the default realm context resolver. The first realm in the list is the default realm. Realms not in this list will be rejected.",
                    "type": "array",
                    "items": {
                        "type": "string"
                    }
                },
                "type": {
                    "description": "The type of realm context resolver to use. Two built-in types are supported: default and test; test is not recommended for production as it does not perform any realm validation.",
                    "type": "string"
                }
            }
        },
        "replicaCount": {
            "description": "The number of replicas to deploy (horizontal scaling). Beware that replicas are stateless; don't set this number \u003e 1 when using in-memory meta store manager.",
            "type": "integer",
            "minimum": 1
        },
        "resources": {
            "description": "Configures the resources requests and limits for polaris pods. We usually recommend not to specify default resources and to leave this as a conscious choice for the user. This also increases chances charts run on environments with little resources, such as Minikube. If you do want to specify resources, uncomment the following lines, adjust them as necessary, and remove the curly braces after 'resources:'.",
            "type": "object"
        },
        "revisionHistoryLimit": {
            "description": "The number of old ReplicaSets to retain to allow rollback (if not set, the default Kubernetes value is set to 10).",
            "type": [
                "integer",
                "null"
            ]
        },
        "service": {
            "description": "Polaris main service settings.",
            "type": "object",
            "properties": {
                "annotations": {
                    "description": "Annotations to add to the service.",
                    "type": "object",
                    "additionalProperties": {
                        "type": "string"
                    }
                },
                "clusterIP": {
                    "description": "You can specify your own cluster IP address If you define a Service that has the .spec.clusterIP set to \"None\" then Kubernetes does not assign an IP address. Instead, DNS records for the service will return the IP addresses of each pod targeted by the server. This is called a headless service. See https://kubernetes.io/docs/concepts/services-networking/service/#headless-services",
                    "type": "string"
                },
                "externalTrafficPolicy": {
                    "description": "Controls how traffic from external sources is routed. Valid values are Cluster and Local. The default value is Cluster. Set the field to Cluster to route traffic to all ready endpoints. Set the field to Local to only route to ready node-local endpoints. If the traffic policy is Local and there are no node-local endpoints, traffic is dropped by kube-proxy.",
                    "type": "string",
                    "enum": [
                        "Cluster",
                        "Local"
                    ]
                },
                "internalTrafficPolicy": {
                    "description": "Controls how traffic from internal sources is routed. Valid values are Cluster and Local. The default value is Cluster. Set the field to Cluster to route traffic to all ready endpoints. Set the field to Local to only route to ready node-local endpoints. If the traffic policy is Local and there are no node-local endpoints, traffic is dropped by kube-proxy.",
                    "type": "string",
                    "enum": [
                        "Cluster",
                        "Local"
                    ]
                },
                "ports": {
                    "description": "The ports the service will listen on. At least one port is required; the first port implicitly becomes the HTTP port that the application will use for serving API requests. By default, it's 8181. Note: port names must be unique and no more than 15 characters long.",
                    "type": "array",
                    "minItems": 1,
                    "items": {
                        "type": "object",
                        "required": [
                            "name",
                            "port"
                        ],
                        "properties": {
                            "name": {
                                "description": "The name of the port. Required.",
                                "type": "string"
                            },
                            "nodePort": {
                                "description": "The port on each node on which this service is exposed when type is NodePort or LoadBalancer. Usually assigned by the system. If not specified or zero, a port will be allocated if this Service requires one. If this field is specified when creating a Service which does not need it, creation will fail.",
                                "type": "integer"
                            },
                            "port": {
                                "description": "The port the service listens on. By default, the HTTP port is 8181.",
                                "type": "integer"
                            },
                            "protocol": {
                                "description": "The IP protocol for this port. Supports \"TCP\", \"UDP\", and \"SCTP\". Default is TCP.",
                                "type": "string",
                                "enum": [
                                    "TCP",
                                    "UDP",
                                    "SCTP"
                                ]
                            },
                            "targetPort": {
                                "description": "Number of the port to access on the pods targeted by the service. If this is not specified or zero, the value of the 'port' field is used.",
                                "type": "integer"
                            }
                        }
                    }
                },
                "sessionAffinity": {
                    "description": "The session affinity for the service. Valid values are: None, ClientIP. The default value is None. ClientIP enables sticky sessions based on the client's IP address. This is generally beneficial to Polaris deployments, but some testing may be required in order to make sure that the load is distributed evenly among the pods. Also, this setting affects only internal clients, not external ones. If Ingress is enabled, it is recommended to set sessionAffinity to None.",
                    "type": "string",
                    "enum": [
                        "None",
                        "ClientIP"
                    ]
                },
                "trafficDistribution": {
                    "description": "The traffic distribution field provides another way to influence traffic routing within a Kubernetes Service. While traffic policies focus on strict semantic guarantees, traffic distribution allows you to express preferences such as routing to topologically closer endpoints. The only valid value is: PreferClose. The default value is implementation-specific.",
                    "oneOf": [
                        {
                            "type": "string",
                            "enum": [
                                "PreferClose"
                            ]
                        },
                        {
                            "type": "null"
                        }
                    ]
                },
                "type": {
                    "description": "The type of service to create. Valid values are: ExternalName, ClusterIP, NodePort, and LoadBalancer. The default value is ClusterIP.",
                    "type": "string",
                    "enum": [
                        "ExternalName",
                        "ClusterIP",
                        "NodePort",
                        "LoadBalancer"
                    ]
                }
            }
        },
        "serviceAccount": {
            "type": "object",
            "properties": {
                "annotations": {
                    "description": "Annotations to add to the service account.",
                    "type": "object",
                    "additionalProperties": {
                        "type": "string"
                    }
                },
                "create": {
                    "description": "Specifies whether a service account should be created.",
                    "type": "boolean"
                },
                "name": {
                    "description": "The name of the service account to use. If not set and create is true, a name is generated using the fullname template.",
                    "type": "string"
                }
            }
        },
        "serviceMonitor": {
            "type": "object",
            "properties": {
                "enabled": {
                    "description": "Specifies whether a ServiceMonitor for Prometheus operator should be created.",
                    "type": "boolean"
                },
                "interval": {
                    "description": "The scrape interval; leave empty to let Prometheus decide. Must be a valid duration, e.g. 1d, 1h30m, 5m, 10s.",
                    "type": "string"
                },
                "labels": {
                    "description": "Labels for the created ServiceMonitor so that Prometheus operator can properly pick it up.",
                    "type": "object",
                    "additionalProperties": {
                        "type": "string"
                    }
                },
                "metricRelabelings": {
                    "description": "Relabeling rules to apply to metrics. Ref https://prometheus.io/docs/prometheus/latest/configuration/configuration/#relabel_config.",
                    "type": "array",
                    "items": {
                        "type": "object"
                    }
                }
            }
        },
        "storage": {
            "description": "Storage credentials for the server. If the following properties are unset, default credentials will be used, in which case the pod must have the necessary permissions to access the storage.",
            "type": "object",
            "properties": {
                "secret": {
                    "description": "The secret to pull storage credentials from.",
                    "type": "object",
                    "properties": {
                        "awsAccessKeyId": {
                            "description": "The key in the secret to pull the AWS access key ID from. Only required when using AWS.",
                            "type": "string"
                        },
                        "awsSecretAccessKey": {
                            "description": "The key in the secret to pull the AWS secret access key from. Only required when using AWS.",
                            "type": "string"
                        },
                        "gcpToken": {
                            "description": "The key in the secret to pull the GCP token from. Only required when using GCP.",
                            "type": "string"
                        },
                        "gcpTokenLifespan": {
                            "description": "The key in the secret to pull the GCP token expiration time from. Only required when using GCP. Must be a valid ISO 8601 duration. The default is PT1H (1 hour).",
                            "type": "string"
                        },
                        "name": {
                            "description": "The name of the secret to pull storage credentials from.",
                            "type": "string"
                        }
                    }
                }
            }
        },
        "tasks": {
            "description": "Polaris asynchronous task executor configuration.",
            "type": "object",
            "properties": {
                "maxConcurrentTasks": {
                    "description": "The maximum number of concurrent tasks that can be executed at the same time. If unspecified or zero, defaults to the number of available cores.",
                    "type": "integer"
                },
                "maxQueuedTasks": {
                    "description": "The maximum number of tasks that can be queued up for execution. If unspecified or zero, defaults to Integer.MAX_VALUE.",
                    "type": "integer"
                }
            }
        },
        "tolerations": {
            "description": "A list of tolerations to apply to polaris pods. See https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/.",
            "type": "array",
            "items": {
                "type": "object"
            }
        },
        "topologySpreadConstraints": {
            "description": "Topology spread constraints for polaris pods. See https://kubernetes.io/docs/concepts/scheduling-eviction/topology-spread-constraints/#topologyspreadconstraints-field.",
            "type": "array",
            "items": {
                "type": "object"
            }
        },
        "tracing": {
            "type": "object",
            "properties": {
                "attributes": {
                    "description": "Resource attributes to identify the polaris service among other tracing sources. See https://opentelemetry.io/docs/reference/specification/resource/semantic_conventions/#service. If left empty, traces will be attached to a service named \"Apache Polaris\"; to change this, provide a service.name attribute here.",
                    "type": "object",
                    "additionalProperties": {
                        "type": "string"
                    }
                },
                "enabled": {
                    "description": "Specifies whether tracing for the polaris server should be enabled.",
                    "type": "boolean"
                },
                "endpoint": {
                    "description": "The collector endpoint URL to connect to (required). The endpoint URL must have either the http:// or the https:// scheme. The collector must talk the OpenTelemetry protocol (OTLP) and the port must be its gRPC port (by default 4317). See https://quarkus.io/guides/opentelemetry for more information.",
                    "type": "string"
                },
                "sample": {
                    "description": "Which requests should be sampled. Valid values are: \"all\", \"none\", or a ratio between 0.0 and \"1.0d\" (inclusive). E.g. \"0.5d\" means that 50% of the requests will be sampled. Note: avoid entering numbers here, always prefer a string representation of the ratio.",
                    "type": "string"
                }
            }
        }
    }
}
